//
// Created by Anuj Puri on 2024-03-15.
//
/**
 * This header file is intended to provide the functionality
 * of verifying the systax of the commands being provided/used
 * by the use for client process.
 */

#ifndef FILE_DOWNLOAD_SERVER_CMD_VERIFICATION_H
#define FILE_DOWNLOAD_SERVER_CMD_VERIFICATION_H
#endif  FILE_DOWNLOAD_SERVER_CMD_VERIFICATION_H


int verify_dir_listing_cmd(char* command, char* option) {

    return 1;
}


int verify_filename_desc_cmd(char* command, char* option) {

    return 1;
}

int verify_file_w_size_cmd(char* command, char* option) {

    return 1;
}

int verify_extn_lst_cmd(char* command, char* option) {

    return 1;
}

int verify_f_bf_date_cmd(char* command, char* option) {

    return 1;
}

int verify_f_af_date_cmd(char* command, char* option) {

    return 1;
}

int verify_quit_client_cmd(char* command, char* option) {

    return 1;
}

int verify(char* command, char* option) {

    return 1;
}